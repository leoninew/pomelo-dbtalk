# Copy this file to backup_databases.yaml and fill in the DSNs.
output_directory: ../data

connections:
  example:
    engine: mysql
    address: "<host>:3306"
    databases:
      - name: "<database>"
        dsn: "mysql+pymysql://<user>:<password>@<host>:3306/<database>"
        enabled: false
